# 文件管理系统工程说明

| 项目 | 内容 |
|------|------|
| 版本 | 1.0 |
| 状态 | MVP 开发中 |
| 编制 | - |
| 发布日期 | 2026-04-16 |

# 1. 项目概述

基于《技术选型报告》构建的文件管理系统，采用前后端一体化架构，Spring Boot + Vue 3 + SQLite技术栈。桌面端本地文件管理工具，提供文件目录管理、文件操作、文件预览、在线Markdown编写功能。

# 2. 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      桌面客户端 (exe)                       │
├──────────────────────┬──────────────────────────────────────┤
│     前端 (Vue3)      │         后端 (Spring Boot)           │
│   ┌──────────────┐  │   ┌────────────────────────────────┐ │
│   │   Views       │  │   │     Controller                │ │
│   │   Components  │  │   │  - DirectoryController       │ │
│   │   Router      │  │   │  - FileController             │ │
│   │   Store       │  │   │  - TrashController            │ │
│   │   API         │  │   │  - PreviewController          │ │
│   └──────────────┘  │   └────────────┬───────────────────┘ │
│                      │                │                     │
│   ┌──────────────┐  │   ┌────────────▼───────────────────┐ │
│   │ Element Plus │  │   │     Service                   │ │
│   │   UI组件库    │  │   │  - DirectoryService          │ │
│   └──────────────┘  │   │  - FileService                │ │
│                      │   │  - TrashService               │ │
│                      │   │  - StorageService             │ │
│                      │   │  - PreviewService              │ │
│                      │   └────────────┬───────────────────┘ │
│                      │                │                     │
│                      │   ┌────────────▼───────────────────┐ │
│                      │   │     Repository                 │ │
│                      │   │  - DirectoryRepository         │ │
│                      │   │  - FileRepository              │ │
│                      │   └────────────┬───────────────────┘ │
│                      │                │                     │
│                      │   ┌────────────▼───────────────────┐ │
│                      │   │     Model (MyBatis-Plus)       │ │
│                      │   │  - Directory                  │ │
│                      │   │  - FileInfo                   │ │
│                      │   └────────────────────────────────┘ │
└──────────────────────┴──────────────────────────────────────┘
                              │
                              ▼
               ┌──────────────────────────────┐
               │     SQLite + 本地文件系统     │
               │   db/fileManager.db         │
               │   storage/files/            │
               └──────────────────────────────┘
```

# 3. 后端架构

## 3.1 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 17 | LTS 版本 |
| Spring Boot | 2.7.x | Web 框架 |
| MyBatis-Plus | 3.5.x | ORM 框架 |
| SQLite | 3.40+ | 嵌入式数据库 |
| Launch4j | - | exe 打包 |

## 3.2 目录结构

```
src/main/java/com/filemanager/
├── Application.java              # 启动类
├── config/                       # 配置层
│   └── DatabaseInitializer.java # 数据库初始化
├── controller/                   # Controller层 (4个)
│   ├── DirectoryController.java # 目录管理
│   ├── FileController.java      # 文件管理
│   ├── TrashController.java     # 回收站
│   └── PreviewController.java   # 预览
├── service/                     # Service层 (5个)
│   ├── DirectoryService.java    # 目录业务
│   ├── FileService.java         # 文件业务
│   ├── TrashService.java        # 回收站业务
│   ├── StorageService.java      # 存储服务
│   └── PreviewService.java      # 预览服务
├── repository/                   # Repository层
│   ├── DirectoryRepository.java
│   └── FileRepository.java
├── model/                       # 数据模型
│   ├── Directory.java           # 目录实体
│   └── FileInfo.java           # 文件实体
├── dto/                         # 数据传输对象
│   └── ...
└── util/                        # 工具类
    └── ...
```

## 3.3 分层约束

```
Controller → Service → Repository → Model
```

| 层级 | 职责 | 禁止操作 |
|------|------|----------|
| Controller | 参数校验 + 响应格式化 + 参数组装 | 禁止执行业务逻辑 |
| Service | 业务逻辑核心，事务边界，可调用多个Repository | 禁止直接访问数据库 |
| Repository | 数据访问层，封装SQL查询 | 禁止执行业务逻辑 |
| Model | 数据模型定义，仅getter/setter | 禁止业务逻辑 |

**禁止路径**：
- ❌ Controller → Repository（应通过Service）
- ❌ Repository → Service（直接查库返回实体）
- ❌ Service → 直接使用JDBC/MyBatis

## 3.4 核心模块

### DirectoryService

| 方法 | 说明 |
|------|------|
| getChildDirectories() | 获取子目录列表 |
| createDirectory() | 创建目录 |
| updateDirectory() | 更新目录 |
| deleteDirectory() | 删除目录（软删除） |
| restoreDirectory() | 恢复目录 |

### FileService

| 方法 | 说明 |
|------|------|
| getFiles() | 获取文件列表 |
| uploadFile() | 上传文件 |
| downloadFile() | 下载文件 |
| deleteFile() | 删除文件（软删除） |
| restoreFile() | 恢复文件 |

### TrashService

| 方法 | 说明 |
|------|------|
| getTrashList() | 获取回收站列表 |
| restore() | 恢复文件/目录 |
| permanentDelete() | 永久删除 |
| clearTrash() | 清空回收站 |
| cleanExpiredItems() | 定时清理（@Scheduled） |

## 3.5 API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/directories | 目录列表 |
| POST | /api/directories | 创建目录 |
| PUT | /api/directories/{id} | 更新目录 |
| DELETE | /api/directories/{id} | 删除目录 |
| GET | /api/files | 文件列表 |
| POST | /api/files/upload | 文件上传 |
| GET | /api/files/{id}/download | 文件下载 |
| DELETE | /api/files/{id} | 删除文件 |
| GET | /api/trash | 回收站列表 |
| POST | /api/trash/{id}/restore | 恢复 |
| DELETE | /api/trash/{id} | 永久删除 |
| DELETE | /api/trash/clear | 清空回收站 |

## 3.6 数据库设计

### 表结构

| 表名 | 说明 |
|------|------|
| directories | 目录表 |
| files | 文件表 |

### 字段规范

| 类型 | 命名 | 示例 |
|------|------|------|
| 主键 | id | id |
| 外键 | entity_id | user_id |
| 名称 | name | name |
| 时间 | *_at | created_at, delete_at |
| 软删除 | deleted | deleted (0/1) |

# 4. 前端架构

## 4.1 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.x | 核心框架 |
| Element Plus | - | UI 组件库 |
| Vite | 5.x | 构建工具 |
| Pinia | - | 状态管理 |
| Vue Router | - | 路由管理 |

## 4.2 目录结构

```
src/
├── main.js                 # 前端入口
├── App.vue                # 根组件
├── api/                   # API 请求封装
│   ├── request.js         # 请求拦截器
│   ├── directory.js       # 目录 API
│   ├── file.js            # 文件 API
│   └── trash.js           # 回收站 API
├── components/            # 公共组件
├── views/                 # 页面视图
│   ├── FileManager.vue    # 文件管理主页面
│   ├── Trash.vue          # 回收站页面
│   └── ...
├── router/                # 路由配置
│   └── index.js
├── store/                 # 状态管理
│   └── ...
└── utils/                 # 工具函数
    └── ...
```

## 4.3 页面模块

| 页面 | 说明 |
|------|------|
| FileManager | 文件管理主页面 |
| Trash | 回收站页面 |

## 4.4 前端约束

| 约束项 | 规则 |
|--------|------|
| 目录结构 | views/页面、components/组件、api/接口、router/路由 |
| 组件通信 | props down, events up，禁止直接修改props |
| 状态管理 | 使用store保存全局状态，组件内状态用ref/reactive |
| API调用 | 统一通过api/目录封装，禁止直接fetch/axios |
| 样式隔离 | scoped styles + BEM命名 |
| 路由懒加载 | 路由使用() => import()实现代码分割 |

# 5. 构建与部署

## 5.1 构建命令

| 命令 | 说明 |
|------|------|
| mvn clean package | 打包后端 |
| mvn spring-boot:run | 开发运行 |
| java -jar file-manager.exe | 生产运行 |

## 5.2 运行时目录

```
file-manager/
├── file-manager.exe      # 主程序
├── db/                  # 数据库
├── storage/             # 文件存储
└── logs/                # 日志
```

# 6. 安全约束

| 规则 | 说明 |
|------|------|
| SQL拼接 | 禁止SQL字符串拼接，使用参数化查询 |
| 明文密码 | 禁止明文存储密码 |
| 文件上传 | 禁止上传可执行文件（.exe/.bat/.jar） |
| 路径遍历 | 防止../路径穿越攻击 |

# 7. 技术框架约束汇总

| 层级 | 技术 | 约束 |
|------|------|------|
| 后端语言 | Java 17 | LTS版本 |
| 后端框架 | Spring Boot 2.7 | 禁止版本冲突 |
| ORM | MyBatis-Plus 3.5 | 禁止直接SQL |
| 前端框架 | Vue 3 + Element Plus | 组件内引入 |
| 编辑器 | Milkdown | 插件化 |
| 构建 | Vite 5 | 开发/构建 |
| DB | SQLite 3.40+ | 单文件 |
| 打包 | Launch4j | exe输出 |

# 8. 版本管理

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0.0 | 2026-04-16 | 初始版本 |
| 1.0.0 | 2026-04-20 | MVP 完成（目录、文件、回收站、预览） |